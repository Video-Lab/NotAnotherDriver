<%- include ('./partials/header') %>
<link rel="stylesheet" href="/public/css/style.css">
<style>
    h1::after {
        content: "";
        display: block;
        width: 50%;
        margin: auto;
        background: #3492eb;
        opacity: 80%;
        height: 3px;
        margin-top: 5px;
    }

    a {
        display: block;
    }
</style>
</head>
<body style="display: flex; flex-direction: column; justify-content: center; align-items: center; height: 100vh;">

<div class="container" style="width: fit-content; text-align: center;">
    <h1 style="font-weight: 300; text-transform: uppercase; letter-spacing: 4px; margin: 1em; font-size: 3em">Report</h1>
    <% if (score.score < 4) { %> 
        <p style="font-weight: 800;">Score: <strong class="score" style="font-size: 2em; color: #e64940; "><%= score.score %></strong> / 10 </p>
    <% } %> <% if(score.score < 7) { %>
        <p style="font-weight: 800;">Score: <strong class="score" style="font-size: 2em; color: #e6a940; "><%= score.score %></strong> / 10 </p>
    <% } %>
    <% if (score.score <= 10) { %>
        <p style="font-weight: 800;">Score: <strong class="score" style="font-size: 2em; color: #2fbd3b; "><%= score.score %></strong> / 10 </p>
    <% } %>
    
    <p style="font-weight: 800;">Suggestions:</p>
    <% if(score.suggestions[0] == "brakes") { %>
        <p><strong>Brakes:</strong> You were braking too close to other cars and objects. Try slowing your descent and planning ahead, especially at high speeds.</p>
    <%}%>
    <% if(score.suggestions[0] == "speed") { %>
        <p><strong>Speed:</strong>You were travelling far from the speed limit at many points during your trip. Make sure to accelerate and decelerate slowly, and stay close to the speed limit.</p>
    <%}%>
    <a href="/scores" class="btn btn-primary" style="font-size: 1em; display: block; margin: 2em auto; width: fit-content;">Back</a>
</div>
<%- include ('./partials/footer') %>